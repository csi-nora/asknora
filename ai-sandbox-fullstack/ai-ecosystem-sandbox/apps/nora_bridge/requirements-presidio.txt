# Optional add-on: Microsoft Presidio PII analyzer/anonymizer for the Nora bridge.
# Not installed by default (keeps the bridge image lean). Enable with:
#
#   1) PRESIDIO_ENABLED=true in .env
#   2) Rebuild: INSTALL_PRESIDIO=1 docker compose -f docker-compose.yml -f docker-compose.proxy.yml build nora-bridge
#      (or: pip install -r apps/nora_bridge/requirements-presidio.txt in a local venv)
#
# Status: GET /sandbox/guardrails/status → presidio.active true|false

presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
