# Lightweight deps for the CSI Nora bridge (no LangChain / heavy ML).
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1
# Server-side, disk-backed Knowledge Base: Postgres registry + full-text (psycopg3).
# Qdrant is reached over its REST API via httpx (no extra client lib needed).
psycopg[binary]==3.2.3
