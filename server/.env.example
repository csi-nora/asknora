# Copy to server/.env and set values. Used by server/gateway.cjs and server/production.cjs.
# Never commit real secrets.

PORT=3456

# Provider keys (server-side — browser does not send keys when Angular backendBaseUrl is set)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
HUGGINGFACE_API_KEY=
