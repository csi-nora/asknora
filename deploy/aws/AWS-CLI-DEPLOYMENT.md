# AWS CLI deployment (moved)

All AWS steps, App Runner vs ECS, S3/CloudFront, and secrets are consolidated here:

**[`../OPERATIONS-RUNBOOK.md`](../OPERATIONS-RUNBOOK.md)** — see **§5 AWS Cloud deployment**.

ECS task definition skeleton remains in this folder: **`ecs-task-definition.example.json`**.
